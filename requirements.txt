# Minimal inferred dependencies for running the analysis scripts
pandas>=1.1
numpy>=1.19
matplotlib>=3.3
scipy>=1.5
scikit-learn>=0.24
# TensorFlow is heavy; choose tensorflow or tensorflow-cpu depending on your setup
tensorflow>=2.6
# Keras is included in tensorflow >= 2.0, but if you use standalone Keras:
# keras>=2.4

# Development / tooling
pre-commit>=2.20
ruff>=0.0.279
pytest>=7.0

